---
const BASE = import.meta.env.BASE_URL;
const year = new Date().getFullYear();
---

<footer class="bg-brand-navy text-white">
  <div class="container mx-auto px-4">
    <div class="grid gap-10 py-14 md:py-16 lg:grid-cols-4">
      <!-- Brand -->
      <div class="lg:col-span-2">
        <div class="flex items-center gap-3">
          <span class="grid h-10 w-10 place-items-center rounded-2xl bg-white/10 ring-1 ring-white/20">
            <span class="h-2 w-2 rounded-full bg-brand-gold"></span>
          </span>
          <div>
            <div class="text-lg font-extrabold tracking-tight">Meli Private Employment Agency</div>
            <div class="text-sm text-white/75">We create jobs; we create careers.</div>
          </div>
        </div>

        <p class="mt-4 max-w-xl text-sm text-white/75 leading-6">
          Trusted foreign employment and recruitment services across multiple sectors.
          We support employers and candidates through a compliant, transparent process.
        </p>

        <div class="mt-6 flex flex-wrap gap-2">
          {["contract","auth","license","screen","medical","qr","travel"].map((t) => (
            <span class="rounded-full bg-white/10 px-3 py-1 text-xs font-extrabold text-white/85 border border-white/10">
              {t}
            </span>
          ))}
        </div>
      </div>

      <!-- Links -->
      <div>
        <div class="text-sm font-extrabold">Quick links</div>
        <div class="mt-4 grid gap-2 text-sm">
          <a class="no-underline text-white/75 hover:text-white" href={`${BASE}services/`}>Services</a>
          <a class="no-underline text-white/75 hover:text-white" href={`${BASE}process/`}>Process</a>
          <a class="no-underline text-white/75 hover:text-white" href={`${BASE}blog/`}>Blog</a>
          <a class="no-underline text-white/75 hover:text-white" href={`${BASE}about/`}>About</a>
          <a class="no-underline text-white/75 hover:text-white" href={`${BASE}contact/`}>Contact</a>
        </div>
      </div>

      <!-- Contact -->
      <div>
        <div class="text-sm font-extrabold">Contact</div>

        <div class="mt-4 grid gap-3 text-sm text-white/75">
          <div class="rounded-2xl bg-white/5 p-4 border border-white/10">
            <div class="text-xs text-white/60">Email</div>
            <div class="mt-1 font-extrabold text-white">info@meli-agency.com</div>
          </div>

          <div class="rounded-2xl bg-white/5 p-4 border border-white/10">
            <div class="text-xs text-white/60">Phone</div>
            <div class="mt-1 font-extrabold text-white">+251 9XX XXX XXX</div>
          </div>

          <div class="rounded-2xl bg-white/5 p-4 border border-white/10">
            <div class="text-xs text-white/60">Office</div>
            <div class="mt-1 font-extrabold text-white">Addis Ababa, Ethiopia</div>
          </div>
        </div>
      </div>
    </div>

    <div class="flex flex-col items-start justify-between gap-3 border-t border-white/10 py-6 text-xs text-white/60 md:flex-row md:items-center">
      <div>© {year} Meli Private Employment Agency. All rights reserved.</div>
      <div class="flex gap-4">
        <a class="no-underline hover:text-white" href={`${BASE}privacy/`}>Privacy</a>
        <a class="no-underline hover:text-white" href={`${BASE}terms/`}>Terms</a>
      </div>
    </div>
  </div>
</footer>
